<header class="header">
    <h1>
        <div class="self-type" data-speed="75">Federico</div>
        <div class="self-type" data-speed="50">Trotta</div>
    </h1>

    <h2 class="self-type" data-speed="25">Bug-hunter & Codesmith</h2>

    <div class="divider self-type" data-speed="10">
        ################################################################
    </div>
</header>

<style>
    .header {
        position: relative;
        text-transform: uppercase;
        padding-block: 3rem;
        background: linear-gradient(180deg, #2e1b00, #f902);
        text-align: center;
        overflow: hidden;
        max-height: 320px;
        transition: all 300ms ease;
    }

    .header .divider {
        position: absolute;
        bottom: 0;
        right: 0;
        transition: all 300ms ease;
    }

    .header h1 > *:nth-child(1),
    .header h1 > *:nth-child(2) {
        line-height: 1;
        transition: all 300ms ease;
    }

    .header h1 > *:nth-child(1) {
        font-size: 3rem;
    }

    .header h1 > *:nth-child(2) {
        font-size: 4.5rem;
        font-weight: 700;
    }

    @media (max-width: 576px) {
        .header h2 {
            font-size: 24px;
        }
    }
</style>
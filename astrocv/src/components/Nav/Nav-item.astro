---
export interface INavItem {
    text?: string;
}

const {
    text = "",
} = Astro.props;
---

<li class={'nav-item ' + text.toLowerCase()}>
  <a class="nav-link" href={'#' + text.toLowerCase()}>{text}</a>
</li>
